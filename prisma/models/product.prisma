// prisma/models/product.prisma
model Product {
  id          String   @id @default(cuid())
  name        String
  price       Float
  image       String
  category    String   // e.g., "Bestsellers"
  isMidnight  Boolean  @default(false)
}